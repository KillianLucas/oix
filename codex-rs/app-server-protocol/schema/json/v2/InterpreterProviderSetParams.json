{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Persist the selected provider to config (affects future turns).",
  "properties": {
    "profile": {
      "description": "Optional config profile to write to instead of the top level.",
      "type": [
        "string",
        "null"
      ]
    },
    "providerId": {
      "type": "string"
    }
  },
  "required": [
    "providerId"
  ],
  "title": "InterpreterProviderSetParams",
  "type": "object"
}